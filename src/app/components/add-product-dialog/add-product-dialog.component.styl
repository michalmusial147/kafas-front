.fillingimg {
  max-width:100%;
  max-height:100%;
  min-width: 100%;
}
.input-file{
  display: none;
}

.input-file-label{
  padding: 25px;
  margin: 25px;
  display: block;
  text-align: center;
  font-weight: bold;
  box-shadow: 0 2px 2px rgba(0, 0, 0, 0.4);
}
.add-product-spinner-wrapper{
  position: fixed;
}
